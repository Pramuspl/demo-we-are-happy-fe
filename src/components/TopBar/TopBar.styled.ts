import styled from "styled-components";

export const TopBarStyled = styled.div`
  position: fixed;
  width: 100%;
  top: 0;
  display: flex;
  flex-direction: row;
  justify-content: flex-end;
  padding: 10px;
  z-index: 10;
`;
